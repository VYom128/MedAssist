/** Inline text link (primary, underlined on hover) with the standard focus ring. */
export const linkClass =
  'rounded font-semibold text-primary-700 hover:underline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600';
